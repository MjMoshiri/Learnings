# css

CSS notes. Layout, sizing, the stuff that bites you when a box renders wider than you expected.

Here so far:

- `box-model.md` — boxes, the four layers, sizing modes, box-sizing, the user agent stylesheet

Start here: `box-model.md`. Everything else builds on the box model.
