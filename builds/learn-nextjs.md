# Learn Next.js

Small blog built to work through Next.js's rendering model: static generation, automatic static optimization, dynamic routes, `getStaticPaths` and `getStaticProps`, ISR, and the basic API routes. Pages router, not App router.

Real takeaway was the build-time vs request-time mental model. Once that clicks, deciding which fetcher to use stops being a coin flip.
